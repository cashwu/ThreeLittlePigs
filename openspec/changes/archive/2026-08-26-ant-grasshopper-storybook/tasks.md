## 1. 建立內容來源檔

- [x] 1.1 [P] 依「`story.md` 為英文來源，`story_zh.json` 為中文來源，`story.json` 為產出的中介檔」的決定，建立 `story.md`：12 個 `## p.<n>` 頁面標題（2、3、4、5、6、7、8、10、12、14、15、16），標題下方編寫完整、連貫且適合兒童跟讀的《The Ant and The Grasshopper》英文故事，保留作者設定的顯示換行與段落間空行，且所有引號一律使用 ASCII 直引號。驗證：`grep -c '^## p\.' story.md` 回傳 12；`grep -n '[“”‘’]' story.md` 無輸出（確認沒有混入彎引號，否則句界判定會失效）；逐頁確認故事連貫、語法正確，並符合既定的 12 頁與 50 句結構。；delivery: story.md；verification: manual assertion「逐頁檢查 story.md 的故事連貫性、語法、12 個頁面標題、50 句、顯示換行、空行及 ASCII 直引號」；regression: N/A（此 primary target 已涵蓋新英文來源檔的完整範圍）；success: 故事連貫且語法正確，並通過 12 個頁面、50 句、關鍵例句與 ASCII 引號檢查；red: N/A（remaining-task：story.md 尚未建立）
- [x] 1.2 [P] 建立 `story_zh.json`，key 為頁碼字串、value 為該頁中文句子陣列。中文由實作者依 `story.md` 的英文分句結果逐句翻譯（舊的 9 句中文寫死在 `generate_audio.py` 內且屬於另一本書，不可沿用）。翻譯粒度 SHALL 與英文分句一一對應，包含句界落在書上一行中間而被拆開的那幾句（例如 p.12 由 `he cried. "And I'm starving, too!` 拆出的兩句，中文也要拆成兩句），否則逐頁句數檢查會擋下。預期句數依序為 4, 4, 3, 5, 3, 5, 5, 4, 7, 5, 3, 2。驗證：以 Python 讀入後印出各 key 的陣列長度，與 `story.json` 中各頁的英文句數逐頁相符且總和為 50；逐句目視確認中文與對應英文句的語意一致。；delivery: story_zh.json；verification: manual assertion「逐句比對 story_zh.json 與 story.md 的 50 個英文分句」；regression: N/A（此 primary target 已涵蓋翻譯語意與逐頁句數的完整範圍）；success: 12 頁逐句語意一致且句數依序為 4, 4, 3, 5, 3, 5, 5, 4, 7, 5, 3, 2；red: N/A（remaining-task：story_zh.json 尚未建立）

## 2. 解析與分句

- [x] 2.1 讓 `generate_audio.py` 滿足「把 story.md 解析為頁與句子」與 "Single source of truth for lines and translations"：把 `story.md` 解析為有序的頁清單，依「以句號分句，換行只保留為顯示資訊」與「用「句尾標點 + 空白 + 大寫或引號」的規則自動分句」兩項決定切句 —— 段落（空行）邊界一律強制斷句，標點規則只在段落內套用，句尾標點後接大寫或左引號才切、接小寫不切，句界落在某個顯示行中間時該行需拆給前後兩句；每個句子產出 `en_lines`（顯示行陣列）而非單一字串，並結合 `story_zh.json` 產出中介檔 `story.json`（頁 → 句 → `{en_lines, zh}`）。不新增只解析不產音的 CLI 旗標，`story.json` 即是檢視分句結果的唯一介面，且 SHALL 在逐頁句數檢查通過後、產生任何音檔之前寫出，使分句結果不必等 50 次語音合成跑完即可檢視。驗證：執行腳本並在音檔開始產生後即可中止，檢視此時已寫出的 `story.json` —— 各頁句數為 4, 4, 3, 5, 3, 5, 5, 4, 7, 5, 3, 2、合計 50；第一個元素的 `page` 為 `"2"`、第一句 `en_lines` 為 `["Once upon a time,", "a little ant lived in a beautiful meadow."]`；p.5 的 `"Why do you bother to work so hard?"` 與 `asked the grasshopper.` 合為一句；p.12 的 `he cried. "And I'm starving, too!` 被拆成兩句且第 5 句 `en_lines` 第二行是 `he cried.`；確認**每一處**段落邊界都產生了句界（例如 p.5 的 `through the long grass.` 與下一段的 `"Why do you bother to work so hard?"` 分屬兩句），不寫死邊界數量。空行規則是防止未來內容誤併的安全網，就本次內容而言段落邊界預期同時也滿足標點規則，因此不需要、也不應該為了驗證它而人工改動 `story.md`。但為了證明段落規則確實有被實作（只實作標點規則的版本會通過本書的所有驗證），SHALL 另以不觸及 `story.md` 的方式直接呼叫分句函式一次，傳入構造字串 `"They ate the seeds.\n\nand slept through the winter."`（前段以句尾標點結束、後段以小寫開頭，標點規則不適用），確認回傳兩個句子。另檢視 `story.json` 確認沒有任何 `en_lines` 元素帶有首尾空白（行中切句處最容易殘留前導空白，會讓瀏覽器端多出一個空 token 而使該句靜默失去高亮）。；delivery: generate_audio.py, story.json；verification: uv run --with edge-tts python generate_audio.py；regression: python -c "import generate_audio as g; assert len(g.split_sentences('They ate the seeds.\\n\\nand slept through the winter.')) == 2"；success: 在第一個音檔產生前寫出含 12 頁、50 句且 en_lines 無首尾空白的 story.json；red: N/A（remaining-task：解析與分句行為尚未實作）
- [x] 2.2 實作 "Fail-fast on count mismatch"：逐頁比對英文句數與 `story_zh.json` 的中文句數，並檢查每個頁碼都存在於 `story_zh.json`；不符時印出含頁碼與兩邊數量的訊息、以非零狀態結束，且不產生任何音檔、`story.json` 或 `index.html`。驗證：暫時從 `story_zh.json` 的某頁刪掉一句執行腳本，確認印出 `Page p.<n>: N English sentences but M Chinese translations`、exit code 非 0、`audio/` 內容與 `index.html` 皆未被更動。；delivery: generate_audio.py；verification: manual assertion「刪除一筆測試翻譯後執行產生器並比對 exit code、錯誤訊息與輸出檔雜湊」；regression: uv run --with edge-tts python generate_audio.py；success: primary target 以非零狀態結束、指出頁碼與兩邊數量，且 audio/、story.json、index.html 均未變更；red: N/A（remaining-task：逐頁 fail-fast 尚未實作）

## 3. 音檔產生

- [x] 3.1 實作 "Generate English audio files" 與 "Capture word boundary timing"：每個句子以 `en_lines` 用單一空白串接後送 edge-tts 產生一個 MP3，並用 `stream()` 收集該句的 word boundary（`word` 與秒為單位的 `offset`）。依「音檔命名採頁面內流水號 `en_pNN_MM.mp3`」的決定命名，頁碼與頁內序號各補零兩位，產出的音檔集合即 `audio/en_pNN_MM.mp3`；在 `index.html` 寫出之後刪除 `audio/` 中所有不屬於本次產生結果的音檔（比對檔名集合，不比對 `en_` 前綴），使目錄內容恰等於本次產生的集合。驗證：`ls audio/en_p*.mp3 | wc -l` 為 50，且 `audio/en_p02_01.mp3`、`audio/en_p10_01.mp3`、`audio/en_p12_07.mp3`、`audio/en_p16_02.mp3` 皆存在；另外手動放一個假的 `audio/zh_99.mp3` 再跑一次，確認它被刪除（用 zh 檔而非 en 檔，才能同時驗到「比對檔名集合而非 `en_` 前綴」這一點）。；delivery: generate_audio.py, audio/；verification: uv run --with edge-tts python generate_audio.py；regression: manual assertion「加入 audio/zh_99.mp3 後重跑並確認孤兒檔被刪除」；success: 產生器成功結束並產生命名正確的 50 個英文 MP3 與逐字 timing；red: N/A（remaining-task：頁內音檔命名、timing 與孤兒清理尚未實作）
- [x] 3.2 移除已淘汰的產生路徑（程式碼層）：刪除 "Generate Chinese audio files" 的實作與腳本內寫死的中文清單，並刪除 "Parse eng.md into lines.json" 的解析邏輯。驗證：`grep -n 'zh-TW\|ZH_VOICE\|zh_.*\.mp3' generate_audio.py` 無輸出（不可用 `grep -c 'zh_'`，因為重寫後的逐頁比對可能出現 `zh_count` 這類區域變數而誤判）；`grep -n 'eng.md\|lines.json' generate_audio.py` 無輸出。；delivery: generate_audio.py；verification: sh -c "! grep -n 'zh-TW\\|ZH_VOICE\\|zh_.*\\.mp3\\|eng.md\\|lines.json' generate_audio.py"；regression: uv run --with edge-tts python generate_audio.py；success: primary target 無輸出且 exit code 為 0；red: N/A（pure-refactor：移除淘汰路徑，不新增獨立行為）

## 4. 網頁產生與互動

- [x] 4.1 依「`index.html` 一律由 `generate_audio.py` 模板產生，手改需回寫模板」的決定，滿足「網頁標題反映目前的繪本」與「產出的網頁與模板一致」：把 commit `4114ad5` 只手改在 `index.html`、未回寫模板的四項現值寫進模板 —— 速度列改為兩顆（`慢慢` 0.33、`正常` 0.5 帶 `active`）、`playbackRate` 初值改 0.5、中文播放按鈕以 HTML 註解形式輸出、`.zh-text` 保留 `margin-left: 50px`；同時把 `<title>` 改為 `The Ant and The Grasshopper - Storybook Practice`、`<h1>` 改為 `The Ant and The Grasshopper`、刪除 `Ryan - pig 2` 副標。驗證：`grep -n '慢2倍\|慢1倍\|0\.75\|Three Little Pigs\|Ryan - pig 2' generate_audio.py` 無輸出；重新產生後 `grep -n 'Three Little Pigs\|Ryan - pig 2' index.html` 無輸出。；delivery: generate_audio.py, index.html；verification: sh -c "! grep -n '慢2倍\\|慢1倍\\|0\\.75\\|Three Little Pigs\\|Ryan - pig 2' generate_audio.py index.html"；regression: manual assertion「檢視重新產生的 index.html 是否保留兩顆速度按鈕、0.5 預設值、中文按鈕註解與 zh-text 縮排」；success: primary target 無輸出且 exit code 為 0；red: N/A（remaining-task：模板仍含舊繪本與舊速度設定）
- [x] 4.2 讓產生的 `index.html` 滿足 "Display all lines with translations"：資料以單一 `PAGES` JavaScript 變數內嵌、不做 runtime fetch；句子卡片依頁分組，每張卡顯示頁內序號、英文句、下方灰色中文；英文句依 `en_lines` 以 `<br>` 排版。驗證：以 `file://` 開啟 `index.html`，畫面正常顯示且 DevTools Network 無任何額外請求；p.2 第一句在畫面上顯示為兩行。；delivery: generate_audio.py, index.html；verification: manual assertion「以 file:// 開啟 index.html 並檢查 PAGES、Network 與 p.2 第一句換行」；regression: N/A（此 primary target 已涵蓋內嵌資料、翻譯、分頁群組與換行的完整相關範圍）；success: 頁面無 runtime fetch、50 張卡片均在 DOM，且 p.2 第一句以兩行顯示；red: N/A（remaining-task：分頁 PAGES 渲染尚未實作）
- [x] 4.3 實作「頁碼 tab 列」與「載入時的預設頁」：標題之下、速度控制之上顯示 12 個 `p.<n>` tab、依書本順序排列、恆有且僅有一個為選中樣式、以 sticky 固定於視窗頂端；載入時預設選中 `p.2`。驗證：以 `file://` 開啟後目視確認 12 個 tab 的標籤與順序為 p.2 p.3 p.4 p.5 p.6 p.7 p.8 p.10 p.12 p.14 p.15 p.16、`p.2` 為選中狀態；捲到 p.12 底部時 tab 列仍可見，且 tab 標籤不與卡片內容重疊、仍清晰可讀；在手機寬度視窗下 tab 列維持一行高、可橫向捲動到其餘 tab、不折行，且 body 不出現水平捲軸。；delivery: generate_audio.py, index.html；verification: manual assertion「在桌面與手機 viewport 檢查 12 個 sticky tabs 的順序、單一選中狀態與橫向捲動」；regression: N/A（此 primary target 已涵蓋 tab 顯示、sticky 與 responsive 行為）；success: tabs 順序正確、預設 p.2、維持一行且 body 無水平捲軸；red: N/A（remaining-task：頁碼 tab 尚未實作）
- [x] 4.4 實作「只有被選中頁的句子可見」：依「tab 切換以顯示/隱藏既有 DOM 實作，並在切頁時停止播放」的決定，一次渲染全部 50 張卡片，切 tab 只切換各頁容器的 `display`，隱藏頁的 DOM 與事件繫結保持不變，並在切頁後把捲動位置回到該頁頂端。DOM id 改為頁內定位 `en-words-p<頁碼兩位>-<頁內序號兩位>`（例如 `en-words-p12-07`），不再使用跨頁流水號。驗證：點 `p.12` 後畫面只出現 7 張卡片、再點回 `p.2` 出現 4 張且播放按鈕皆為 ▶；在 p.12 捲到底再點 `p.16`，確認畫面停在該頁第一張卡片；DevTools 確認隱藏頁的卡片仍存在於 DOM 中，且 `document.getElementById('en-words-p12-07')` 取得得到元素。；delivery: generate_audio.py, index.html；verification: manual assertion「切換 p.12、p.2、p.16 並檢查可見卡片、scroll 與隱藏 DOM id」；regression: manual assertion「切回已瀏覽頁後事件繫結與播放按鈕仍可用」；success: 一次僅一頁可見、切頁回到頁首，且 en-words-p12-07 持續存在；red: N/A（remaining-task：頁容器顯示切換尚未實作）
- [x] 4.5 實作「切換頁面會停止播放」並維持 "Single active playback"：切 tab（含點擊當前已選中的 tab）時呼叫 `stopCurrent()`，讓播放中或暫停中的音訊停止、按鈕回 ▶、高亮清除；同時全站任一時刻至多一個音訊為 active。驗證：播放 p.2 某句後點 `p.4`，確認聲音停止；切回 `p.2` 該句按鈕為 ▶ 且無高亮，點擊後從頭播放。；delivery: generate_audio.py, index.html；verification: manual assertion「播放或暫停任一句後切換 tab（含目前 tab）」；regression: manual assertion「播放另一句時仍停止前一句並維持單一 active audio」；success: 音訊停止、按鈕回到 ▶、currentTime 歸零且高亮清除；red: N/A（remaining-task：切頁停止播放尚未實作）
- [x] 4.6 實作 "Canonical tokenization and alignment" 與 "Word-by-word karaoke highlighting"：依「karaoke 的 `<span>` 索引跨 `<br>` 連續編號」的決定，把 `en_lines` 以空白串接後再以空白切分產生 token，`<span>` 索引跨 `<br>` 連續不重置；依「word boundary 併入 `PAGES`，不保留獨立的全域 `TIMINGS`」的決定，把每句的時間資料掛在該句物件的 `timings` 欄位上、不產生以全域索引定位的獨立陣列；token 數與 `timings` 長度不符時該句退回**保留 `<br>` 換行的**純文字、無高亮但仍可播放，且不影響其他句子 —— 不可沿用現況模板的 `enHtml = line.en`（單一字串），那會在 fallback 時把多行併成一行而丟失 `story.md` 顯示換行。驗證：`grep -n 'TIMINGS' index.html` 無輸出（不可只查 `const TIMINGS`，否則 `WORD_TIMINGS`、`let TIMINGS` 等寫法會讓檢查空轉）；在 DevTools console 確認 `typeof PAGES` 為 `object` 且逐字時間只能自 `PAGES` 的句子物件取得；播放 p.2 第一句與 p.12 第 5 句，確認高亮跨越換行後仍依序落在正確的字上、且高亮的字與唸出的字一致；在 console 檢查各句 `span` 數與其 `timings` 長度，統計不符的句數並記錄。；delivery: generate_audio.py, index.html；verification: manual assertion「檢查 PAGES timings、跨 br span 索引、高亮與 mismatch fallback」；regression: sh -c "! grep -n 'TIMINGS' index.html"；success: timing 僅存在 PAGES 句子物件，匹配句跨行依序高亮，不匹配句保留換行且可播放；red: N/A（remaining-task：兩層 timing 與跨行 token 對齊尚未實作）
- [x] 4.7 維持 "Play English audio per line" 與 "Playback speed control" 的既有行為，並確認 "Play Chinese audio per line" 已移除：英文播放按鈕指向該句的 `audio/en_p<page>_<index>.mp3`；速度按鈕為「慢慢」(0.33) 與「正常」(0.5)、預設「正常」、切換即時生效且跨頁保持；中文播放按鈕維持 HTML 註解狀態、畫面上不出現。驗證：在 p.12 點第 7 句的播放鍵，DevTools Network 顯示載入 `audio/en_p12_07.mp3`；選「慢慢」後切到 p.4 播放仍為 0.33 倍速；在 DevTools console 執行 `document.querySelectorAll('.play-btn.zh').length` 得到 0。；delivery: generate_audio.py, index.html；verification: manual assertion「播放 p.12 第 7 句、跨頁切換速度並查詢中文播放按鈕」；regression: manual assertion「驗證播放／暫停與 single active playback」；success: 載入 en_p12_07.mp3、0.33 倍速跨頁保持且 .play-btn.zh 數量為 0；red: N/A（remaining-task：新頁面資料模型的播放與速度行為尚未接通）

## 5. 產生、清理與整體驗證

- [x] 5.1 以 uv 載入 edge-tts 執行 `generate_audio.py` 完整跑一次產生流程，產出 50 個英文音檔、`story.json` 與 `index.html`，且 exit code 為 0。若中途因連線中斷或服務節流而失敗，staging 目錄會被清除，正式音檔與舊 `index.html` 保持不變，直接重跑整支腳本即可；孤兒檔清除仍排在寫出新 `index.html` 之後。驗證：指令成功結束；`ls audio/en_p*.mp3 | wc -l` 為 50；`story.json` 與 `index.html` 的修改時間為本次執行。；delivery: audio/, story.json, index.html；verification: uv run --with edge-tts python generate_audio.py；regression: sh -c "test $(find audio -maxdepth 1 -name 'en_p*.mp3' | wc -l) -eq 50 && test -s story.json && test -s index.html"；success: primary target exit code 為 0 並印出完成產生訊息；red: N/A（remaining-task：完整產生流程尚未成功執行）
- [x] 5.2 在 5.1 成功之後才刪除腳本不負責的舊繪本來源檔：刪除 `eng.md` 與 `lines.json`。舊的全域流水號音檔與中文音檔由 3.1 的孤兒檔清除步驟負責，此處只做驗證，不重複人工刪除。驗證：`ls eng.md lines.json` 回報找不到檔案；`ls audio/zh_*.mp3 audio/en_[0-9][0-9].mp3` 回報找不到檔案（確認 3.1 的清除確實生效，若仍存在代表腳本漏了清除步驟）；`ls audio | wc -l` 為 50，確認 `audio/` 內容恰等於本次產生的集合。；delivery: eng.md, lines.json, audio/；verification: sh -c "test ! -e eng.md && test ! -e lines.json && test $(find audio -maxdepth 1 -type f | wc -l) -eq 50 && ! find audio -maxdepth 1 -type f \\( -name 'zh_*.mp3' -o -name 'en_[0-9][0-9].mp3' \\) | grep -q ."；regression: uv run --with edge-tts python generate_audio.py；success: primary target exit code 為 0 且舊來源與舊音檔不存在；red: N/A（pure-refactor：成功產生後移除淘汰來源與確認清理結果）
- [x] 5.3 完成端到端手動驗收：以 `file://` 開啟 `index.html`，逐一確認標題為 `The Ant and The Grasshopper` 且無副標、12 個 tab 可切換且捲動時維持可見、預設在 p.2、各頁卡片數為 4/4/3/5/3/5/5/4/7/5/3/2、切頁後捲動位置在該頁頂端、p.12 第 5 句顯示為兩行且第 6 句以 `"And I'm starving, too!` 開頭、播放/暫停切換正常、暫停時高亮保留、切頁會停止播放。驗證：以上每一項目視確認通過，並把 karaoke 失準（token 數與 `timings` 長度不符）的句子清單記錄下來。；delivery: index.html；verification: manual assertion「依 task 5.3 清單以 file:// 完成桌面與手機端到端驗收」；regression: manual assertion「逐句統計 token 與 timings 不符清單，並覆核播放、暫停、速度與切頁停止」；success: task 5.3 列出的每一項 UI、內容、卡片數、scroll 與播放行為均目視通過；red: N/A（remaining-task：端到端驗收尚未執行）
